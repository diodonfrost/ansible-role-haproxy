---
services:
  - docker

env:
#-----------------------------------------------
#
# CentOS
#
#-----------------------------------------------
  - distribution: centos
    version: "7"
    playbook: default.yml
  - distribution: centos
    version: "7"
    playbook: frontend.yml
  - distribution: centos
    version: "7"
    playbook: listen.yml

  - distribution: centos
    version: "6"
    playbook: default.yml
  - distribution: centos
    version: "6"
    playbook: frontend.yml
  - distribution: centos
    version: "6"
    playbook: listen.yml
#--------------------------------
#
# Fedora
#
#--------------------------------
  - distribution: fedora
    version: "28"
    playbook: default.yml
  - distribution: fedora
    version: "28"
    playbook: frontend.yml
  - distribution: fedora
    version: "28"
    playbook: listen.yml

  - distribution: fedora
    version: "27"
    playbook: default.yml
  - distribution: fedora
    version: "27"
    playbook: frontend.yml
  - distribution: fedora
    version: "27"
    playbook: listen.yml

  - distribution: fedora
    version: "26"
    playbook: default.yml
  - distribution: fedora
    version: "26"
    playbook: frontend.yml
  - distribution: fedora
    version: "26"
    playbook: listen.yml

#--------------------------------
#
# Ubuntu
#
#--------------------------------
  - distribution: ubuntu
    version: "18.04"
    playbook: default.yml
  - distribution: ubuntu
    version: "18.04"
    playbook: frontend.yml
  - distribution: ubuntu
    version: "18.04"
    playbook: listen.yml

  - distribution: ubuntu
    version: "17.10"
    playbook: default.yml
  - distribution: ubuntu
    version: "17.10"
    playbook: frontend.yml
  - distribution: ubuntu
    version: "17.10"
    playbook: listen.yml

  - distribution: ubuntu
    version: "16.04"
    playbook: default.yml
  - distribution: ubuntu
    version: "16.04"
    playbook: frontend.yml
  - distribution: ubuntu
    version: "16.04"
    playbook: listen.yml

  - distribution: ubuntu
    version: "14.04"
    playbook: default.yml
  - distribution: ubuntu
    version: "14.04"
    playbook: frontend.yml
  - distribution: ubuntu
    version: "14.04"
    playbook: listen.yml

  - distribution: ubuntu
    version: "12.04"
    playbook: default.yml
  - distribution: ubuntu
    version: "12.04"
    playbook: frontend.yml
  - distribution: ubuntu
    version: "12.04"
    playbook: listen.yml

#--------------------------------
#
# Debian
#
#--------------------------------
  - distribution: debian
    version: "9"
    playbook: default.yml
  - distribution: debian
    version: "9"
    playbook: frontend.yml
  - distribution: debian
    version: "9"
    playbook: listen.yml

  - distribution: debian
    version: "8"
    playbook: default.yml
  - distribution: debian
    version: "8"
    playbook: frontend.yml
  - distribution: debian
    version: "8"
    playbook: listen.yml

#--------------------------------
#
# Oraclelinux
#
#--------------------------------
  - distribution: oraclelinux
    version: "7"
    playbook: default.yml
  - distribution: oraclelinux
    version: "7"
    playbook: frontend.yml
  - distribution: oraclelinux
    version: "7"
    playbook: listen.yml

  - distribution: oraclelinux
    version: "6"
    playbook: default.yml
  - distribution: oraclelinux
    version: "6"
    playbook: frontend.yml
  - distribution: oraclelinux
    version: "6"
    playbook: listen.yml

#--------------------------------
#
# Amazonlinux
#
#--------------------------------
  - distribution: amazonlinux
    version: "2"
    playbook: default.yml

#--------------------------------
#
# Opensuse
#
#--------------------------------
  - distribution: opensuse
    version: "42.3"
    playbook: default.yml
  - distribution: opensuse
    version: "42.3"
    playbook: frontend.yml
  - distribution: opensuse
    version: "42.3"
    playbook: listen.yml

  - distribution: opensuse
    version: "42.2"
    playbook: default.yml
  - distribution: opensuse
    version: "42.2"
    playbook: frontend.yml
  - distribution: opensuse
    version: "42.2"
    playbook: listen.yml

#--------------------------------
#
# Archlinux
#
#--------------------------------
  - distribution: archlinux
    version: "rolling"
    playbook: default.yml
  - distribution: archlinux
    version: "rolling"
    playbook: frontend.yml
  - distribution: archlinux
    version: "rolling"
    playbook: listen.yml

before_install:
  # Build docker container
  - 'sudo docker pull diodonfrost/${distribution}-${version}-ansible:latest'

script:
  - container_id=$(mktemp)

  # Run container and mount ansible role with it
  - 'sudo docker run --detach --privileged -v /sys/fs/cgroup:/sys/fs/cgroup:ro --volume="${PWD}":/etc/ansible/roles/ansible-role-haproxy:ro diodonfrost/${distribution}-${version}-ansible:latest > "${container_id}"'

  # Check syntax
  - 'sudo docker exec "$(cat ${container_id})" env ANSIBLE_FORCE_COLOR=1 ansible-playbook -v /etc/ansible/roles/ansible-role-haproxy/tests/playbooks/${playbook} --syntax-check'

  # Run ansible playbook
  - 'sudo docker exec "$(cat ${container_id})" env ANSIBLE_FORCE_COLOR=1 ansible-playbook -v /etc/ansible/roles/ansible-role-haproxy/tests/playbooks/${playbook}'

  # Test indempotence
  - >
    sudo docker exec "$(cat ${container_id})" env ANSIBLE_FORCE_COLOR=1 ansible-playbook -v /etc/ansible/roles/ansible-role-haproxy/tests/playbooks/${playbook}
    | grep -q 'changed=0.*failed=0'
    && (echo 'Idempotence test: pass' && exit 0)
    || (echo 'Idempotence test: fail' && exit 1)
  # Test role
  - 'inspec exec tests/inspec -t docker://$(cat ${container_id})'

  # Delete container
  - 'sudo docker rm -f "$(cat ${container_id})"'

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
